# Learn How to Love — Cursor / Hermes 程式規範

> 本檔為程式落地鎖定規範。敘事／世界觀見 `MEMORY.md`；完整設計見 `guide_line.md`。  
> **Playable 基線（2026-08）：** `Ch1_Trust_Version3/Renpy_game/`（Ren'Py）  
> **舊 Web** `Ch1_Trust/game/`（HTML／`scenes.js`）— **已不在本倉庫**，勿當落地目標。

---

## 1. 專案結構

```
Learn_How_To_Love/
├── MEMORY.md                 ← 世界觀鎖定（Hermes 必讀）
├── guide_line.md             ← 系列聖經
├── agent/                    ← Cursor Agent Skills
├── Ch1_Trust_Version3/       ← **Ch1 主程式（改這裡）**
│   ├── Renpy_game/game/      ← script.rpy／screens／endings…
│   ├── Renpy_game/tools/     ← validate-*.py
│   ├── assets/               ← bg／dog／char／audio
│   └── agents/               ← section 文案、game_guild、gamer
├── Ch2_StillHere/ · Ch3_Goodbye/
└── tools/hermes/             ← 編排與 Nous Portal 本地門戶
```

**禁止：** 從使用者指定的 backup 目錄擅自刪除或當產線來源（見 `guide_line` §二·6）。

---

## 2. 語言與文案

- 玩家可見文字：**繁體中文、台灣用語**（禁簡體、禁大陸用語）
- 敘述方法：**婉轉、不直白**——用動作／距離／感官／願不願意暗示；禁講破機制、結局收法、睡姿、信任數值；導引與選單不劇透
- 狗名：**禁止寫死** → Ren'Py 用 `[dog_label]`／`dog_label`；取名前多用「牠」
- 身世半隱：對齊 `MEMORY.md` §3.1／§4.1／§4.2 — Ch1 半句＋行為，禁車禍閃回／前主人全史／收集 UI

---

## 3. Ren'Py 落地契約（現行）

- 主敘事／選項在 `Ch1_Trust_Version3/Renpy_game/game/script.rpy`（及 `endings.rpy`／`hidden_content.rpy` 等）
- 信任／結局條件對齊 `agents/game_guild.md`
- 新段必對 section 設計稿 `agents/section_*.md`
- 人／狗 zoom 必對 `Ch1_Trust_Version3/agents/image_scale.md`（母尺：後門人 0.42／狗 0.44）
- 狗名：`default dog_label`；`renpy.input` 須保留 `screen input`
- Ch2 種子旗標：`ch2_seed_*`（含 `ch2_seed_dog_had_someone`）

### 舊 HTML 契約（僅供對照／若將來復原）

`scenes.js`／`choice-reactions.js`／`locations.js` 規格仍見 git 歷史與舊 skill；**現行勿寫入不存在的 `Ch1_Trust/game/`。**

---

## 4. flags 命名

- 語意清楚（Ren'Py 存檔常用 snake／既有 `s0N_*`、`ch2_seed_*`）
- 禁止：`flag1`、`temp`、`x`
- 新增 flag 須註明：誰寫入、誰讀取
- Landmark／跨作種子寫入後不可當可刷完美用途

---

## 5. 資產與路徑

| 類型 | 路徑（Version3） | 規則 |
|------|------------------|------|
| 狗 pose | `assets/dog/`（＋子目錄動畫幀） | 獨立疊層，不畫進背景 |
| 背景 | `assets/bg/bg-{slug}.png` | 對齊 `agents/image_bg.md` |
| 人／狗尺 | `script.rpy` transforms | 對齊 `agents/image_scale.md` |
| 角色 | `assets/char/` | 予安局部／不露全臉原則見 visual-art |
| BGM／SFX | `assets/audio/` | 登記 `CREDITS.md` |

---

## 6. 驗證（章節落地必跑）

```powershell
cd Ch1_Trust_Version3\Renpy_game
python tools\validate-s01.py
python tools\validate-s10.py
python tools\validate-all-endings.py
python tools\validate-menus.py
# 依變更再加：validate-assets／validate-narration／validate-menu-layout
```

---

## 7. Git 與變更紀律

- **最小必要 diff** — 不順手重構無關檔案
- **API 金鑰** — 以 `Nous_Portal.md` 為準；值寫入 `tools/hermes/.env`（勿提交）
- 僅在使用者明確要求時 `git commit`／`git push`
- LHTL 遊戲變更以 **`Ch1_Trust_Version3/`** 為準

---

## 8. Agent 分工

| 任務 | Skill / 工具 |
|------|----------------|
| 世界觀／場景架構 | `agent/story-narrative/` · `MEMORY.md` |
| 對白語氣 | `agent/tw-narrative-voice/` |
| 角色／身世邊界 | `agent/character-bible/` |
| 美術 PNG | `agent/visual-art/` |
| 音效 | `agent/audio-sound/` |
| 測試 | `agent/game-tester/` · `Ch1_Trust_Version3/agents/tester.md` |
| 背景批次產 | Hermes + Nous Portal |
| 引擎／UI 精修 | **Cursor**（本 IDE） |

---

## 9. 開啟遊戲

- Ren'Py：`Ch1_Trust_Version3/Renpy_game/開啟遊戲.bat`（或專案慣用啟動）
- **每次修改後，Agent 預設不得自動開遊戲**
- 僅在使用者明確說「開遊戲」「測試」「play」時才啟動

---

## 10. 衝突解決順序

```
MEMORY.md（世界觀鎖定）
 → guide_line.md
 → Ch1_Trust_Version3/agents/game_guild.md
 → Ch1_Trust_Version3/agents/image_scale.md（人／狗 zoom）
 → agent/story-narrative
 → 其餘 agent skills
 → 本 .cursorrules
```

---

*最後更新：2026-09-05 · Playable＝Ch1_Trust_Version3 Ren'Py*
